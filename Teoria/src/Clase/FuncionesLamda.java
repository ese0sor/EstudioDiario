package Clase;

public class FuncionesLamda {

	/**
	 * 	? -> Es una condicion de si se cumple lo anterior se hace lo siguiente.
	 * 			listaFiguras.add(tirarMoneda() ? 
	 * 							new Rectangulo(aleatorio(50), aleatorio(50)) :
	 * 							new Cuadrado(aleatorio(50)));
	 * 
	 * 	: -> 
	 * 
	 * 	; -> Cerramos la funcion.
	 */
}
